import React from 'react';
import { Typography, Container, Paper } from '@mui/material';

const Profile = () => {
  return (
    <Container>
      <Paper sx={{ p: 3 }}>
        <Typography variant="h4">User Profile</Typography>
        <Typography>Profile functionality coming soon...</Typography>
      </Paper>
    </Container>
  );
};

export default Profile;