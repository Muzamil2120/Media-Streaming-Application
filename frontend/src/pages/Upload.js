import React from 'react';
import { Typography, Container } from '@mui/material';

const Upload = () => {
  return (
    <Container>
      <Typography variant="h4">Upload Page</Typography>
      <Typography>Media upload functionality coming soon...</Typography>
    </Container>
  );
};

export default Upload;